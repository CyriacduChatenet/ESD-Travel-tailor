import { FC } from "react";

import { SigninForm } from "@/app/components/signinFom";

export const SigninPage: FC = () => {
    return (
        <div>
            <h1>Signin page</h1>
            <SigninForm/>
        </div>
    );
};